apiVersion: v1
kind: Service
metadata:
  name: backend      # must stay "backend" — nginx.conf proxies to http://backend:5000/
  namespace: super-meal
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
